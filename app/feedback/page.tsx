'use client';import {useState} from 'react';export default function Page(){const[type,setType]=useState('اقتراح أداة');const[msg,setMsg]=useState('');const[done,setDone]=useState(false);async function copy(){await navigator.clipboard.writeText(`[${type}]\n${msg}`);setDone(true)}return <div className="container page-head legal"><h1>الملاحظات والاقتراحات</h1><p>لا يوجد Backend لاستقبال البيانات في النسخة الحالية، لذلك لن نوهمك بأن النموذج يُرسل. جهّز رسالتك هنا وانسخها بنقرة واحدة.</p><div className="content-card"><label>النوع<select value={type} onChange={e=>setType(e.target.value)}><option>اقتراح أداة</option><option>بلاغ عن خطأ</option><option>ملاحظة عامة</option></select></label><label>الرسالة<textarea maxLength={1000} value={msg} onChange={e=>setMsg(e.target.value)} placeholder="اكتب التفاصيل هنا..."/></label><button className="btn primary" disabled={!msg.trim()} onClick={copy}>{done?'تم النسخ':'نسخ الرسالة'}</button></div></div>}
