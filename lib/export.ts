export function safeFileName(input:string,fallback='design'){return (input||fallback).trim().replace(/[^\p{L}\p{N}_-]+/gu,'-').replace(/^-+|-+$/g,'').slice(0,60)||fallback}
export async function canvasToPng(canvas:HTMLCanvasElement,name:string){const blob=await new Promise<Blob|null>(r=>canvas.toBlob(r,'image/png',1));if(!blob)throw new Error('تعذر إنشاء PNG');downloadBlob(blob,`${safeFileName(name)}.png`)}
export async function canvasToPdf(canvas:HTMLCanvasElement,name:string,widthMm:number,heightMm:number){const {jsPDF}=await import('jspdf');const orientation=widthMm>heightMm?'landscape':'portrait';const pdf=new jsPDF({orientation,unit:'mm',format:[widthMm,heightMm],compress:true});pdf.addImage(canvas.toDataURL('image/png'),'PNG',0,0,widthMm,heightMm,undefined,'FAST');pdf.save(`${safeFileName(name)}.pdf`)}
function downloadBlob(blob:Blob,name:string){const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=name;a.rel='noopener';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1500)}
