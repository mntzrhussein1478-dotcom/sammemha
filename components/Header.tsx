'use client';
import Link from 'next/link';
import {useEffect,useRef,useState} from 'react';
import ThemeToggle from './ThemeToggle';
export default function Header(){const[open,setOpen]=useState(false);const ref=useRef<HTMLElement>(null);useEffect(()=>{function close(e:MouseEvent){if(open&&ref.current&&!ref.current.contains(e.target as Node))setOpen(false)}function key(e:KeyboardEvent){if(e.key==='Escape')setOpen(false)}document.addEventListener('pointerdown',close);document.addEventListener('keydown',key);return()=>{document.removeEventListener('pointerdown',close);document.removeEventListener('keydown',key)}},[open]);return <header className="site-header" ref={ref}><div className="container nav"><Link className="brand" href="/" aria-label="صمّمها - الرئيسية" onClick={()=>setOpen(false)}><span className="brand-mark" aria-hidden="true">ص</span><span>صمّمها</span></Link><nav id="main-navigation" className={open?'nav-links open':'nav-links'} aria-label="التنقل الرئيسي"><Link href="/tools" onClick={()=>setOpen(false)}>الأدوات</Link><Link href="/about" onClick={()=>setOpen(false)}>عن المنصة</Link><Link href="/changelog" onClick={()=>setOpen(false)}>التحديثات</Link></nav><div className="nav-actions"><ThemeToggle/><button className="icon-btn mobile-only" aria-expanded={open} aria-controls="main-navigation" aria-label={open?'إغلاق القائمة':'فتح القائمة'} onClick={()=>setOpen(v=>!v)}>{open?'×':'☰'}</button></div></div></header>}
